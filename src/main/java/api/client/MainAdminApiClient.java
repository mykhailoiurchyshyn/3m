package api.client;

import utils.Environment;

public class MainAdminApiClient extends AbstractApiClient {
    public MainAdminApiClient(Environment env, String authToken) {
        super(env, authToken);
    }
}
